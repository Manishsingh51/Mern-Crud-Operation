
# To clone the application
git clone git@github.com:Manishsingh51/Mern-Crud-Operation.git

# To run the backend application 
first go to the rest-sevicce using cd rest-service
npm install
npm run dev

# To run the client application
open the new terminal
cd frontend
npm install
npm dev run

copy the url from terminal and open the browser and paste this url in browser.